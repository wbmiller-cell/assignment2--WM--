# assignment2--WM--
Assignment 2 Description
- I used the penguins.csv dataset for the assignment
- The data analysis script is named "WM_Individual_Assn_2.do". The script imports the penguins.csv dataset, cleans up the data by telling Stata to treat any "NA" inputs as missing, and runs descriptive statistics for the quantitative and categorical variables.
- To reproduce my analysis, one would simply re run the tab1 command in Stata for the categorical variables (island, sex, year, and species) and re run the summarize command (su) for the quantitative variables (the remaining ones). 

